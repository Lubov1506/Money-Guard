export const selectTransactions = (state) => state.items;
export const selectIsLoading = (state) => state.loading;
export const selectError = (state) => state.error;
export const selectCurrentTransaction = (state) => state.currentTransaction;
